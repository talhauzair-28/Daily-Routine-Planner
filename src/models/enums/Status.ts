/**
 * Status types for activities and tasks
 * @enum {string}
 */
export enum Status {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
  MISSED = 'missed',
  DELAYED = 'delayed',
}

