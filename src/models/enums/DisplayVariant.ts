/**
 * Display variants for different component layouts
 * @enum {string}
 */
export enum DisplayVariant {
  FULL = 'full',
  COMPACT = 'compact',
  MINIMAL = 'minimal',
  SUMMARY = 'summary',
  CIRCULAR = 'circular',
}

